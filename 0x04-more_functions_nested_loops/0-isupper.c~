#include "holberton.h"
#include <stdio.h>

/**
*main - check the code for ALX School students.
*
*Return: Always 0.
*/

int _isupper(int c)
{
  int c;

  if (c <= 'A' && c >= 'Z');
  {
    _putchar(1);
  }
  return (0);
}
